import { defineConfig } from '@williamthorsen/node-monorepo-core';

/**
 * nmr configuration for this monorepo.
 *
 * The built-in defaults already match this repo's conventions.
 * Override `rootScripts` or `workspaceScripts` here to customize.
 */
export default defineConfig({});
