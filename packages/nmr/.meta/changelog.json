[
  {
    "version": "0.23.0",
    "date": "2026-08-01",
    "sections": [
      {
        "title": "🎉 Features",
        "audience": "all",
        "items": [
          {
            "description": "Close the shared Vitest config's path gaps (#546)",
            "body": "Defines common patterns to determine which files are considered test files and which are subject to code coverage. Consumers that use `defineVitestConfig` benefit from these patterns if they follow naming and file-location conventions.",
            "breaking": true,
            "hash": "9db1a65ad484e43fb214e958cb7599274cde5986"
          }
        ]
      },
      {
        "title": "🐛 Bug fixes",
        "audience": "all",
        "items": [
          {
            "description": "Stop rewriting documented commands inside Markdown fences (#549)",
            "body": "Fixes an issue where `nmr fmt` silently rewrote shell commands documented in a repository's Markdown, leaving usage examples that no longer ran as written. Code fences inside Markdown are now left untouched in any language, withdrawing the fence formatting added in 0.22.0. Fence formatting can be reenabled on selected paths via the config.",
            "hash": "a3e6b2030bff82683b79e2c72f9924bbef941949"
          }
        ]
      }
    ]
  },
  {
    "version": "0.22.0",
    "date": "2026-07-31",
    "sections": [
      {
        "title": "🎉 Features",
        "audience": "all",
        "items": [
          {
            "description": "Ship shell and Dockerfile formatting from a shared nmr Prettier config (#540)",
            "body": "Adds the ability to format shell scripts and Dockerfiles to `nmr fmt`. `@williamthorsen/nmr` now provides a common config for consistency across projects. Shell fences inside Markdown are now formatted as well. These features are absorbed into `nmr fmt` and `nmr fmt:check`; the `fmt:sh` and `fmt:all` commands are removed.",
            "breaking": true,
            "hash": "e87669bc70834d0314c01f3ebb02c330faa4150f"
          },
          {
            "description": "Require the monorepo root in the shared root Vitest config (#541)",
            "body": "Modifies the testing configuration so that root-level test runs always cover the repository containing the configuration. The shared root Vitest config now requires a `monorepoRoot` option specifying an absolute path to the repository's own root, in place of the optional search origin. A repository nested inside a parent monorepo fails instead of resolving silently to its parent.",
            "breaking": true,
            "hash": "87bbe7129cf3ccac99bc85790654c1e6e86a4602"
          }
        ]
      }
    ]
  },
  {
    "version": "0.21.0",
    "date": "2026-07-30",
    "sections": [
      {
        "title": "🎉 Features",
        "audience": "all",
        "items": [
          {
            "description": "Add a shared Vitest config export (#526)",
            "body": "Adds a shared Vitest configuration that can be used by repos that use `nmr`. Tests are now sorted into three named categories (unit, integration, and app), so that a run can target or skip a single category. A second variant excludes all tests in workspace packages, allowing the monorepo's own root-level tests to be run separately.",
            "hash": "2674aaeaa1f650f3df0ed46974bb29f59d9a2c63"
          },
          {
            "description": "Retire the Vitest config-file variants from nmr's defaults (#533)",
            "body": "Packages no longer need extra Vitest config files to keep integration tests out of the default test run. Integration tests are now recognized by an `*.int.test.ts` suffix, and every package gets the same five commands: `test`, `test:all`, `test:coverage`, `test:integration`, and `test:watch`. The same five commands work from the monorepo root. A Vitest run that collects no test files now passes instead of failing. Consumers should migrate the Vitest config when upgrading.",
            "breaking": true,
            "hash": "c47a283a1a22a07582a61886fcce811d5933c607"
          }
        ]
      },
      {
        "title": "🐛 Bug fixes",
        "audience": "all",
        "items": [
          {
            "description": "Apply ignore rules from any working directory when formatting (#535)",
            "body": "Fixes an issue where `nmr fmt` and `nmr fmt:check` did not apply a pattern from a package's `.gitignore` or `.prettierignore` when the run started at the monorepo root, so files a package excluded could fail a root-level format gate while passing a package-scoped one. Untracked files that git does not ignore are now formatted alongside tracked ones, and a run started outside a git repository now fails instead of reporting a clean pass over nothing. Trailing arguments to `nmr fmt` are git pathspecs rather than Prettier flags; one that matches no formattable file now fails instead of exiting silently.",
            "hash": "4591a17581c03b6d635ead1bb224af223095379b"
          }
        ]
      },
      {
        "title": "⚙️ Tooling",
        "audience": "dev",
        "items": [
          {
            "description": "Adopt Vitest projects in this repo (#530)",
            "body": "Simplifies test configurations so that every package exposes the same test commands, which select suites by test category: `nmr test` runs everything except integration tests, `nmr test:integration` runs only those, and `nmr test:all` runs both. `nmr test:integration` now succeeds in a package that has no integration tests instead of failing. release-kit's drift checks can now be run on their own, apart from unit and integration tests. A fresh clone now has to run `pnpm run bootstrap` before any test run, not just before `nmr` commands.",
            "hash": "a2fdd9c03c419f8fb70941ff5a3c580a6badcde6"
          }
        ]
      }
    ]
  },
  {
    "version": "0.20.0",
    "date": "2026-07-28",
    "sections": [
      {
        "title": "🎉 Features",
        "audience": "all",
        "items": [
          {
            "description": "Migrate to code-quality workflow v7 and retire the version-restatement apparatus (#503)",
            "body": "Migrates the code-quality workflow from `@v6` to `@v7`, which no longer requires the Node version to be specified in two places, and removes the test that previously checked for drift. Also removed are the `nmr sync-pnpm-version` command and drift-related exported functions.\n\nTaking this upgrade requires migrating the repo's code-quality workflow to `@v7` and deleting its version-alignment test in the same change.",
            "breaking": true,
            "hash": "afb20acb7195d06b3d7a34c4fe6d0555f3bc553c"
          },
          {
            "description": "Resolve workspace packages with pnpm's pattern semantics (#509)",
            "body": "nmr now excludes packages matched by a `!`-prefixed pattern in `pnpm-workspace.yaml` from the packages it operates on. Deep globs such as `packages/**` now match nested packages, and running a command from inside a nested package targets that package rather than an enclosing ancestor. Exclusion patterns must be quoted to take effect. nmr now requires Node.js 24.16 or later.",
            "breaking": true,
            "hash": "023dce5e648a7d9139a7e87eb63ec87cdb6dc08b"
          },
          {
            "description": "Report pnpm overrides when reviewing dependencies (#514)",
            "body": "From the monorepo root, `nmr upgrade` now reports the repo's active pnpm dependency overrides alongside the upgrade report, making clear when a pin is the reason an expected upgrade is absent. Repos that wired up this override report themselves with an install-time hook can now remove that hook.",
            "hash": "2d780d663edbc3645c9f7feb5c150a71c80261f2"
          },
          {
            "description": "Trim the bundled agent guidance to a cheatsheet (#516)",
            "body": "Trims the agent guidance nmr installs into consuming repos from 100 lines to 10. Only the bare essentials are now injected into context. Agents are instructed to consult the package's README for details.",
            "hash": "6e63e9f5a86e44b4c4d25983e6c178ee573ce4d5"
          }
        ]
      },
      {
        "title": "🐛 Bug fixes",
        "audience": "all",
        "items": [
          {
            "description": "Anchor script execution to the resolved registry's directory (#517)",
            "body": "Fixes an issue where `nmr --workspace-root`, run from inside a workspace package, either failed outright or silently acted on the wrong directory. Commands now behave the same regardless of the directory they are invoked from: root commands act on the monorepo root, and package commands on their own package. Two changes for existing users: `nmr --workspace-root clean` now sweeps every workspace package instead of cleaning only the current one, and relative paths passed to a script now resolve against the directory the script runs in.",
            "hash": "37bd74dbe4fa4f459ef7e50a9aa944384128160d"
          }
        ]
      }
    ]
  },
  {
    "version": "0.19.0",
    "date": "2026-07-22",
    "sections": [
      {
        "title": "🎉 Features",
        "audience": "all",
        "items": [
          {
            "description": "Replace the dependency commands with nmr upgrade (#491)",
            "body": "Adds `nmr upgrade` as an ergonomic wrapper for displaying and applying available dependency upgrades. Taze is used under the hood to set a soaking period (customizable, defaulting to 7 days) before recently published dependencies will be accepted.\n\nRemoves the four dependency commands it replaces: `outdated`, `outdated:latest`, `update`, and `update:latest`.",
            "breaking": true,
            "hash": "c18ba9f8d1fef780cb5f0d114f2dde2af0efe935"
          }
        ]
      },
      {
        "title": "📦 Dependencies",
        "audience": "dev",
        "items": [
          {
            "description": "Upgrade dependencies and align the Node support policy (#483)",
            "body": "All four published packages (`nmr`, `nmr-core`, `release-kit`, and `v11y-check`) now require Node.js 24 or later, up from Node 18.17. Separately, `nmr-compile` now rebuilds when the TypeScript version changes.",
            "hash": "3090aa1ec68224f88e0b537ae09282e8e636f711"
          }
        ]
      }
    ]
  },
  {
    "version": "0.18.2",
    "date": "2026-07-18",
    "sections": [
      {
        "title": "🐛 Bug fixes",
        "audience": "all",
        "items": [
          {
            "description": "Fail the build on aliased imports that escape the package source (#475)",
            "body": "Fixes an issue where an aliased import that resolved to a target outside the package's own source (one Node could not reach at runtime) passed the build but produced published output that crashed with a module-not-found error. `nmr-compile` now fails the build and reports the unresolvable import and where it pointed, so the problem surfaces before publish instead of in production. Aliased imports that resolve outside the source but remain genuinely external and runnable continue to build and ship unchanged.",
            "hash": "2f6fe3575bb1bdcca7c29923780a357ac54408be"
          }
        ]
      }
    ]
  },
  {
    "version": "0.18.1",
    "date": "2026-07-14",
    "sections": [
      {
        "title": "🐛 Bug fixes",
        "audience": "all",
        "items": [
          {
            "description": "Fix the stale build cache, the unshipped rimraf dependency, and the aborting root clean (#471)",
            "body": "Fixes the following issues:\n- `nmr clean` failed with a missing-command error in any project that had not separately installed `rimraf`, a tool nmr neither declared nor shipped.\n- If run from a monorepo root, `nmr clean` cleaned one package and then aborted, leaving every other package untouched. Only repos that build nmr from source were affected.\n- If run from a monorepo root, `nmr clean` ignored a package's own `clean` script.\n- `nmr compile` skipped the build for a package whose output had been deleted, because the sources were unchanged. The package compiled to an empty output directory with no error and no warning, and could be published as an empty tarball.\n- `nmr compile` reported missing output and recompiled on every run for a package whose sources emit nothing, such as a source tree of only declaration files.",
            "hash": "6b416b49e043dde2daf040e6852cd1a12285b339"
          }
        ]
      }
    ]
  },
  {
    "version": "0.18.0",
    "date": "2026-07-13",
    "sections": [
      {
        "title": "🐛 Bug fixes",
        "audience": "all",
        "items": [
          {
            "description": "Remove the attw check and the nmr-attw binary (#469)",
            "body": "Fixes an issue where a repo using nmr's default scripts could not pass `check:strict` without installing `@arethetypeswrong/cli` and conforming to inflexible requirements of questionable value. The `attw` check is removed.",
            "hash": "57bce420a3fb1ebb0a988f9c13e7c6da79c07b3d"
          }
        ]
      }
    ]
  },
  {
    "version": "0.17.2",
    "date": "2026-07-06",
    "sections": [
      {
        "title": "🐛 Bug fixes",
        "audience": "all",
        "items": [
          {
            "description": "Replace the raw attw script with an nmr-attw wrapper (#461)",
            "body": "Fixes the issues that made the default `nmr attw` check fail across monorepos:\n\n- packages with no published API failed with a false `NoResolution` error;\n- the check left stray `.tgz` files in the working tree ;\n- output was a verbose per-package table; now output is terse on success, full diagnostics only on failure (`--verbose` to force full output);\n- a missing `@arethetypeswrong/cli` gave a raw command-not-found; now a hint is given.",
            "hash": "ecf92884637bb43007b715597064ecc5ccd7a88a"
          }
        ]
      }
    ]
  },
  {
    "version": "0.17.1",
    "date": "2026-07-01",
    "sections": [
      {
        "title": "🐛 Bug fixes",
        "audience": "all",
        "items": [
          {
            "description": "Relocate nmr-compile's build cache out of the published dist (#457)",
            "body": "Fixes an issue where packages built with nmr-compile shipped an internal build-cache file inside their published npm tarball.",
            "hash": "871fe03628a19cbf9eb854b8246d358a1b99653f"
          }
        ]
      }
    ]
  },
  {
    "version": "0.17.0",
    "date": "2026-07-01",
    "sections": [
      {
        "title": "🎉 Features",
        "audience": "all",
        "items": [
          {
            "description": "Rebuild nmr-compile on a unified tsc emit (#455)",
            "body": "`nmr` now compiles a package's JavaScript and type declarations in one step, rewriting every import form — static, re-export, dynamic `import()`, bare side-effect, and tsconfig `paths` aliases — to runnable `.js` in both outputs. It now requires TypeScript 5.7 or newer as a peer dependency.",
            "breaking": true,
            "hash": "0e55822c88cad341f90b052b15fb9af41e7ae414"
          }
        ]
      }
    ]
  },
  {
    "version": "0.16.0",
    "date": "2026-06-30",
    "sections": [
      {
        "title": "🎉 Features",
        "audience": "all",
        "items": [
          {
            "description": "Rewrite parseArgs on node:util with typed error and exit API (#428)",
            "body": "`@williamthorsen/nmr-core` gains a typed error API for CLI parsing: `parseArgsOrExit` (parse, or print a usage error and exit) and `ParseError` replace the per-command boilerplate, so every bundled CLI reports a flag mistake the same way. Breaking: the `translateParseError` export is removed and `parseArgs` now throws `ParseError`.",
            "breaking": true,
            "hash": "5629bef8f645485b401a04f06ff9153ef5472282"
          },
          {
            "description": "Auto-activate integration test variant from config presence (#448)",
            "body": "A package can now separate its integration tests from its standalone suite simply by including a `vitest.integration.config.ts` (alongside a `vitest.standalone.config.ts`). The `--int-test` flag that previously enabled this is removed — that config-file pairing is now the only way to activate the separation. In such a package, `test` and `test:coverage` run only the standalone suite and skip integration tests, while a new `test:all` runs both suites together. The separation now holds even when tests run across every package at once, so a full-workspace `test` run still keeps integration tests out of the default suite. Packages that previously hand-copied these test scripts no longer need to.",
            "breaking": true,
            "hash": "ee320b913ef1d7255c78eef6fc9ab3b9e94dfd39"
          }
        ]
      },
      {
        "title": "🐛 Bug fixes",
        "audience": "all",
        "items": [
          {
            "description": "Prevent CLI output truncation when piped before exit (#446)",
            "body": "Fixes an issue where large output from the `nmr` and `v11y` commands could be truncated when captured through a pipe — for example, by a CI job.",
            "hash": "fa7eeb12c1739b4bce3a73d9143cea4dd967b291"
          }
        ]
      },
      {
        "title": "♻️ Refactoring",
        "audience": "dev",
        "items": [
          {
            "description": "Migrate ensure-prepublish-hooks to nmr-core parseArgs (#429)",
            "body": "`ensure-prepublish-hooks` now parses its arguments with `nmr-core`'s shared `parseArgs` utilities instead of a hand-rolled `for`/`switch` loop, removing the last bespoke argument parser in the `nmr` package. Every `nmr` CLI now shares one argument-parsing and error-reporting path; as a side effect, stray positional arguments are ignored rather than rejected, while unknown flags and a missing `--command` value still exit non-zero.",
            "hash": "6860c19d06f4068c249f6ecbe2e69906ba24155c"
          },
          {
            "description": "Route all CLI error reporting through a single stderr helper (#437)",
            "body": "Consolidates error reporting across the CLIs so every command reports errors the same way, and guards against the previous inconsistency returning. The error messages and exit codes users see are unchanged.",
            "hash": "63e36b3f80bd8858e940362ea2914cb9a19fd821"
          },
          {
            "description": "Normalize CLI error wording to the canonical Error: format (#439)",
            "body": "Single-line error messages from the `nmr` and `release-kit` commands now print in one uniform shape, so the same class of failure reads the same way no matter which command produced it. Previously the wording varied, which made error output harder to scan and harder for scripts to match against. Richer output, such as multi-line validation reports and the stack trace from an unexpected crash, is unchanged.",
            "hash": "2cbb46523fc7c3f0d8387405cafbd2cc90274b19"
          }
        ]
      }
    ]
  },
  {
    "version": "0.15.0",
    "date": "2026-06-27",
    "sections": [
      {
        "title": "🎉 Features",
        "audience": "all",
        "items": [
          {
            "description": "Centralize the per-package build as an nmr-compile bin (#419)",
            "body": "Introduces `nmr-compile`, a single command shipped with `@williamthorsen/nmr` that compiles each workspace package and now backs the default build. Consuming repos can delete their own per-package build script and pick up future build fixes just by upgrading nmr. Repeated builds with unchanged source now reliably skip recompiling instead of occasionally rebuilding for no reason, and import aliases now resolve correctly in symlinked checkouts.",
            "hash": "cc20e3130087689c2fe8bf0e6de4b1349646296e"
          }
        ]
      },
      {
        "title": "🐛 Bug fixes",
        "audience": "all",
        "items": [
          {
            "description": "Write the build cache only after a successful compile (#421)",
            "body": "Fixes an issue where a package compile that failed partway — from a crash, disk error, or transient build failure — could leave stale or incomplete output that the next build skipped over as unchanged. The next build now retries the failed compile, so recovering no longer requires a manual `nmr clean` or a source-file edit.",
            "hash": "424a3e684f0fa6db97ee58f4f032afc123df8327"
          },
          {
            "description": "Stop sync-agent-files --check failing on version-only bumps (#424)",
            "body": "Fixes an issue where `nmr sync-agent-files --check` failed after an nmr upgrade even when the managed agent guidance was identical. The check now passes whenever the guidance content is current, even if the version has changed.",
            "hash": "f6ab59827d3fd1fd9e031d996c1dbb84ce5b3113"
          },
          {
            "description": "Remove inapplicable bootstrap fallback from agent guidance (#425)",
            "body": "Fixes an issue where the agent guidance bundled with nmr told coding agents to run a recovery step that exists only in the nmr repo itself.",
            "hash": "3a730b802a1cbd17c029a651ade07fa6ec841c22"
          }
        ]
      },
      {
        "title": "♻️ Refactoring",
        "audience": "dev",
        "items": [
          {
            "description": "Sync workflow pnpm version via yaml document API (#412)",
            "body": "`nmr sync-pnpm-version` now updates every pinned pnpm version in a workflow, not just the first, so a workflow that pins the version across multiple jobs no longer leaves later jobs on a stale version.",
            "hash": "f3dc04d429fc6b68c5d589d6fa566e4e3c2be964"
          }
        ]
      }
    ]
  },
  {
    "version": "0.14.2",
    "date": "2026-06-16",
    "sections": [
      {
        "title": "♻️ Refactoring",
        "audience": "dev",
        "items": [
          {
            "description": "Migrate from js-yaml to yaml (#410)",
            "body": "Replaces the `js-yaml` dependency with the `yaml` package for all YAML reading and writing, with no change to behavior or generated output. The swap trims the dependency footprint by dropping a direct dependency and its companion type-definitions package, since `yaml` ships its own types.",
            "hash": "fec315ab67e6d5c4b7eac3cb908c7c83a323f17c"
          }
        ]
      }
    ]
  },
  {
    "version": "0.14.1",
    "date": "2026-05-19",
    "sections": [
      {
        "title": "♻️ Refactoring",
        "audience": "dev",
        "items": [
          {
            "description": "Restructure tests and align core package directory with package name (#405)",
            "body": "Tests in every package are now typechecked alongside the code they cover, so type breakage in tests fails the build instead of slipping through. The `core` package's workspace directory is renamed to match its package name, so `nmr -F nmr-core ...` and `pnpm --filter nmr-core ...` now resolve where they previously failed.",
            "hash": "91006ec4281be902d66dfdef3917f603ee56b5c3"
          }
        ]
      },
      {
        "title": "🧪 Tests",
        "audience": "dev",
        "items": [
          {
            "description": "Eliminate subprocess-startup flakiness in CLI tests (#403)",
            "body": "The `nmr` CLI test suite now passes deterministically and no longer leaks subprocess output to the vitest terminal. Production CLI behavior — output streaming, exit codes, error messages — is unchanged.",
            "hash": "35f68339c60b30319d8593ee5fd09a21f287f93b"
          }
        ]
      }
    ]
  },
  {
    "version": "0.14.0",
    "date": "2026-05-10",
    "sections": [
      {
        "title": "⚙️ Tooling",
        "audience": "dev",
        "items": [
          {
            "description": "Expose workspace bins from any subdir and simplify nmr docs (#374)",
            "body": "Workspace bins like `nmr`, `release-kit`, and `audit-deps` are now invocable from any subdirectory under the monorepo when direnv is active, replacing the previous root-only `.envrc` recipe. The nmr usage docs are restructured to present two unambiguous resolution modes — direnv (recommended) and `pnpm exec nmr` — and the repo's README now documents direnv as the recommended setup.",
            "hash": "96e6f23c2256e7c7856cbfdf3b2a8753b987e226"
          }
        ]
      }
    ]
  },
  {
    "version": "0.13.0",
    "date": "2026-05-04",
    "sections": [
      {
        "title": "🎉 Features",
        "audience": "all",
        "items": [
          {
            "description": "Add :pre and :post hook conventions to nmr commands (#339)",
            "body": "Adds `:pre` and `:post` hook conventions to nmr's command runner. Consumers can declare `X:pre` or `X:post` scripts that nmr runs automatically before and after script `X`. The hook commands are optional and ignored if missing. Hooks fire even when the main command is overridden, and direct invocations like `nmr build:pre` are treated as leaf operations rather than recursively cascading into `build:pre:pre` lookups.",
            "hash": "e6f0d918131eed1637b0e8336dd8371a471b9751"
          },
          {
            "description": "Filter `nmr --help` to nmr commands (#348)",
            "body": "Restricts `nmr --help` to nmr commands only. Hook scripts (names ending in `:pre` or `:post`) are now hidden from help, and generic `package.json` lifecycle scripts (`prepare`, `postinstall`, `bootstrap`) no longer appear. When a `package.json` script overrides a built-in nmr command, the override is shown inline alongside the command name with a `*` marker, and a single `* Overridden by package.json` footnote is appended whenever any marker is rendered. Help also now reflects the active resolution context: invoked from a subpackage you see workspace-level overrides; invoked from the repo root (or with `-w`) you see root-level overrides.",
            "hash": "929daf7640e62c873bd8111b2ec7c85b97fd74fd"
          }
        ]
      },
      {
        "title": "🐛 Bug fixes",
        "audience": "all",
        "items": [
          {
            "description": "Honor -w flag in composite-script step subprocesses (#346)",
            "body": "Fixes an issue where invoking `nmr -w <command>` from inside a workspace package, with `<command>` resolving to a composite (multi-step) script defined at the root level, failed with `Unknown command` for any step that was reachable only via the root script registry. The `-w` flag is now propagated to every step's subprocess invocation, so composite scripts run end-to-end regardless of which directory they are invoked from.",
            "hash": "5f1c6fa32aef392db5ad88c3782be2043ab6e84d"
          }
        ]
      },
      {
        "title": "♻️ Refactoring",
        "audience": "dev",
        "items": [
          {
            "description": "Read package version at runtime via shared helper (#338)",
            "body": "Fixes an issue where running `audit-deps`, `nmr`, or `release-kit` from the locally built `dist/esm/` after a `git pull` could report a stale version. Each CLI now reads its version directly from its `package.json` at startup, so version reads stay in sync with the installed source without requiring a fresh `pnpm install` or rebuild.",
            "hash": "0fc98bb70d458d8eb5727d9567e3e20a8f2ded00"
          }
        ]
      }
    ]
  },
  {
    "version": "0.12.0",
    "date": "2026-04-23",
    "sections": [
      {
        "title": "🎉 Features",
        "audience": "all",
        "items": [
          {
            "description": "Add agent-facing AGENTS.md and sync-agent-files command (#263)",
            "body": "Ships nmr-owned agent guidance alongside the `@williamthorsen/nmr` package so consuming repos stop hand-maintaining their own copies of the runner's invocation rules. A new `nmr sync-agent-files` command pulls that guidance into `.agents/nmr/AGENTS.md` in the consuming repo, stamped with the installed nmr version; a companion `--check` variant verifies the stamp against the installed version on every root `check:strict` run. Drift between installed nmr and the committed guidance now fails the quality gate automatically with a single actionable fix message, without per-consumer wiring.",
            "hash": "aaab0b7dc8b1f21b35159d935d199a84839ac71a"
          },
          {
            "description": "Rename check:fixable script to fix:check (#266)",
            "body": "Renames the `check:fixable` convenience script to `fix:check` in both the workspace and root default script registries. The new name mirrors the existing `fmt` / `fmt:check` pattern, aligning the read-only variant with its mutating counterpart (`fix`) so the command's read-only semantics are recognizable from the name alone.\n\nThe script's expansion (`fmt:check`, `lint:check`) is unchanged.",
            "hash": "4595dc1d1f2ef5943bfc2640d4883ed852ecec41"
          }
        ]
      }
    ]
  },
  {
    "version": "0.11.0",
    "date": "2026-04-17",
    "sections": [
      {
        "title": "🎉 Features",
        "audience": "all",
        "items": [
          {
            "description": "Add `check:fixable` convenience script (#223)",
            "body": "Adds a new `check:fixable` convenience script to the `nmr` default script registries, providing a read-only partner to the existing `fix` script. Running `nmr check:fixable` expands to `nmr fmt:check && nmr lint:check`, verifying that a tree is clean of auto-fixable violations without modifying any files.",
            "hash": "d75dfc03dec6d594168b1a2d5b9415e3d127a360"
          },
          {
            "description": "Show package name in override-script messages (#226)",
            "body": "Adds the package directory name as a prefix to override-script log messages, making it easy to identify which package each message belongs to when running commands across multiple workspaces. Also introduces a distinct no-op (`:`) condition that logs a skip message and exits cleanly, separate from the existing empty-string case.",
            "hash": "6e850b9df1e4a361d8489e5d91dedbf08eb1baef"
          },
          {
            "description": "Migrate nmr audit scripts to use audit-deps (#234)",
            "body": "Migrates nmr's built-in `audit:dev` and `audit:prod` root scripts from calling `audit-ci` directly via `pnpm dlx` to delegating to the `audit-deps` CLI wrapper via `pnpm exec audit-deps`. Removes the now-obsolete `.config/audit-ci/` directory and its JSON5 config files, since `audit-deps` uses its own config at `.config/audit-deps.config.json`. Updates the nmr readyup kit to check for `@williamthorsen/audit-deps` installation instead of checking for legacy audit-ci directory placement.",
            "hash": "39dbd3ed5e8b9aa47b5652c6847fefa5e75a141b"
          }
        ]
      }
    ]
  },
  {
    "version": "0.10.0",
    "date": "2026-04-16",
    "sections": [
      {
        "title": "🎉 Features",
        "audience": "all",
        "items": [
          {
            "description": "Decouple audit from CI quality gate and add audit workflow (#210)",
            "body": "Dependency audit is now decoupled from the CI quality gate so that transient upstream CVEs no longer block the merging of unrelated code changes. Audit now runs in a dedicated workflow with non-blocking PR integration (acknowledgment checkbox) and a daily scheduled run that tracks results in a standing GitHub issue. A readyup kit is available to validate the new setup in consuming repos.",
            "hash": "05615b37e66c9a7293022141d95c3502055eeb2d"
          }
        ]
      }
    ]
  },
  {
    "version": "0.9.2",
    "date": "2026-04-15",
    "sections": [
      {
        "title": "⚙️ Tooling",
        "audience": "dev",
        "items": [
          {
            "description": "Enable automated publication to npm (#187)",
            "body": "Prepares the repository for reliable tag-triggered npm publishing by adding missing package metadata, standardizing licensing, and introducing a readyup kit that validates publish readiness across all packages.",
            "hash": "89536cbcb5bb9aa30620bd262dce2c99f84ee0bc"
          }
        ]
      }
    ]
  },
  {
    "version": "0.9.0",
    "date": "2026-04-04",
    "sections": [
      {
        "title": "🎉 Features",
        "audience": "all",
        "items": [
          {
            "description": "Add --version flag to nmr and release-kit (#143)",
            "body": "Adds `--version` / `-V` support to the `nmr` and `release-kit` CLIs, matching the existing `preflight` behavior. Moves the build-time version generation script to the shared `config/` directory so all three packages use a single `generateVersion.ts`.",
            "hash": "7257a8eb37a622e0065f105d09f41541b76c7d8e"
          },
          {
            "description": "Add devBin config for source-repo binary substitution (#146)",
            "body": "Adds a `devBin` config field to nmr that maps binary names to replacement commands, with relative paths resolved from the monorepo root. Documents `\":\"` as the recommended way to disable a script when `eslint-plugin-package-json/valid-scripts` forbids empty strings.",
            "hash": "4a369abab767df1940a50412910d86827c1a9395"
          },
          {
            "description": "Detect and report missing build output in bin wrappers (#152)",
            "body": "Adds try/catch with `ERR_MODULE_NOT_FOUND` detection to all six bin wrappers across `nmr`, `preflight`, and `release-kit`. Previously, five of the six wrappers used bare `import()` calls that produced cryptic unhandled rejections when `dist/` was missing, and `preflight`'s existing try/catch gave no actionable guidance.",
            "hash": "1e7bdbe21e66c61762286da695aeab7afa9ec115"
          }
        ]
      },
      {
        "title": "📚 Documentation",
        "audience": "dev",
        "items": [
          {
            "description": "Refine README to match preflight documentation standard (#137)",
            "body": "Rewrites the nmr README to match the documentation standard established by the preflight README (#114). Restructures content to follow the cross-package convention (header → installation → quick start → concepts → CLI reference), adds comprehensive reference tables for CLI flags, `defineConfig` fields, and all built-in script registries (workspace and root), and introduces visual aids for context-aware resolution and three-tier override precedence.",
            "hash": "b480e58385fa5f98874ee73e2a0ec307afdb0edf"
          }
        ]
      }
    ]
  },
  {
    "version": "0.5.0",
    "date": "2026-03-31",
    "sections": [
      {
        "title": "🎉 Features",
        "audience": "all",
        "items": [
          {
            "description": "Add `fix` script to workspace and root registries (#106)",
            "body": "Adds a `fix` composite script to both the workspace and root script registries in nmr. The script runs `lint` then `fmt` in sequence, providing a single command to auto-fix linting and formatting issues.",
            "hash": "e638b95968153a832684310c4b1e3075d47fb383"
          }
        ]
      }
    ]
  },
  {
    "version": "0.4.0",
    "date": "2026-03-30",
    "sections": [
      {
        "title": "🎉 Features",
        "audience": "all",
        "items": [
          {
            "description": "Add default root scripts and split registry module (#96)",
            "body": "Adds four new default root scripts to nmr (`fmt:sh`, `fmt:all`, `clean`, `root:check`) and split the monolithic `registries.ts` into a data-only `default-scripts.ts` and a composition-logic `resolve-scripts.ts`.\n\nAlso fixes the `ci` and `check:strict` script ordering to run build before strict checks, and corrects stale test assertions.",
            "hash": "348e2addfa2405203672a8a7b577ddc289f12e01"
          }
        ]
      }
    ]
  },
  {
    "version": "0.3.0",
    "date": "2026-03-29",
    "sections": [
      {
        "title": "🎉 Features",
        "audience": "all",
        "items": [
          {
            "description": "Resolve package.json scripts at root level and skip in recursive mode (#95)",
            "body": "Pass `monorepoRoot` as `packageDir` when `isRoot` is true so root `package.json` scripts participate in the same override tier that workspace packages already use.\n\nSet `NMR_RUN_IF_PRESENT=1` in the `-R` codepath so child processes that can't resolve a command exit 0 (skip) instead of exit 1.\n\nOnly log \"Using override script\" when the `package.json` script actually overrides a registry command.\n\nRemove the self-referencing `ci: \"nmr ci\"` script from root `package.json` and update the CI workflow to use `code-quality-pnpm-workflow.yaml@v4` with an explicit `check-command`.",
            "hash": "4f0b43ba8355f51aaf53c9e454ada08ab98c6452"
          }
        ]
      }
    ]
  },
  {
    "version": "0.2.0",
    "date": "2026-03-28",
    "sections": [
      {
        "title": "🎉 Features",
        "audience": "all",
        "items": [
          {
            "description": "Add ensure-prepublish-hooks binary (#75)",
            "body": "New binary that checks whether all publishable (non-private) workspace packages have a `prepublishOnly` script, and optionally adds one.\n\n- Check mode (default): reports each non-private package's `prepublishOnly` status, exits non-zero if any are missing.\n- Fix mode (`--fix`): inserts `prepublishOnly` into packages that lack it. Supports `--dry-run`.\n- Custom command (`--command`): overrides the default hook value (`npm run build`).\n\nAlso adds `private` field extraction to the shared `PackageJson` interface.",
            "hash": "a7480c4c6bfff2e4b271f6eb9f79837e31aeac60"
          }
        ]
      },
      {
        "title": "📚 Documentation",
        "audience": "dev",
        "items": [
          {
            "description": "Document utility binaries",
            "body": "Add README sections for the package's additional commands. Subcommands (report-overrides, sync-pnpm-version) are documented under \"Additional subcommands\" with nmr invocation syntax. The standalone ensure-prepublish-hooks utility is documented separately under \"Standalone utilities\".\n\nAlso fix the executable bit on bin/ensure-prepublish-hooks.js to match the other bin entries.",
            "hash": "b34ef80a6e95b5cc4dbd101259658f133a3ff3f9"
          }
        ]
      }
    ]
  },
  {
    "version": "0.1.1",
    "date": "2026-03-28",
    "sections": [
      {
        "title": "🎉 Features",
        "audience": "all",
        "items": [
          {
            "description": "Extract nmr CLI from core package (#61)",
            "body": "Extracts all nmr CLI code from `packages/core` into a new `packages/nmr` package (`@williamthorsen/nmr`). Core is reduced to an empty shared-library shell ready for cross-cutting utilities. All internal references are rewired and the full build/test pipeline passes.\n\nScopes: core, nmr",
            "hash": "d1abff0286f593a24444c0113d0286b4e70d6973"
          }
        ]
      },
      {
        "title": "♻️ Refactoring",
        "audience": "dev",
        "items": [
          {
            "description": "Extract helpers to reduce duplication in config and consistency modules (#62)",
            "body": "Extracts two small helpers to consolidate structurally duplicated code in the nmr package. A new `getStringFromYamlFile` helper in `tests/helpers/` replaces the repeated YAML-read-parse-extract pattern in `consistency.ts`, and a private `validateScriptField` helper in `config.ts` replaces the duplicated script-record validation blocks.",
            "hash": "369cc7280442bde612a6cfa40603984e7c02b241"
          }
        ]
      }
    ]
  }
]
